import React from 'react';
import Logo from '../../Logo/Logo';
import './styles.css'

const Toolbar = (props) => (
    <header className="Toolbar">
        <div>MENU</div>
        <Logo />
        <nav>
            ...
        </nav>
    </header>
);

export default Toolbar;